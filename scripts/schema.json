{
  "type": "object",
  "title": "Related Brands (Auto)",
  "properties": {
    "heading": {
      "type": "string",
      "title": "Heading",
      "default": "Related brands"
    },
    "maxBrands": {
      "type": "number",
      "title": "Max brands to show",
      "default": 12,
      "minimum": 1,
      "maximum": 36
    },
    "dataUrl": {
      "type": "string",
      "title": "Data source URL",
      "format": "uri-reference",
      "default": "/content/related-brands.json",
      "description": "Path to the JSON map produced by your generator"
    }
  },
  "required": ["dataUrl"],
  "ui:order": ["heading", "maxBrands", "dataUrl"]
}
